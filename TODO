Übersetzungen hinzufügen
Bei Registrierung checken, ob die Email bereits benutzt wird. Ansonsten Fehler melden
border-bottom färbt sich erst, wenn das passwort mit dem ersten übereinstimmt 
better warning if the otp is ???
fix the submit button of otp even if there is an invalid type of code

Man muss bei jeder registrierung das Elternteil abmelden und sofort wieder bei anmelden, ohne dass das elternteil was tun muss. sonst kann das elternteil im falschen account sein

Bei den Abfragen der Students lässt sich mit "customuser_set" das Elternteil abfragen



fortschrittsbalken für passwort reset und registrierung

farben auf der teacher profilseite

forms.py - Register_OTP
Clear the fields if any error arises

register_otp.html - scripts
In das Script muss noch herein, dass beim Kopieren der Text auf die Fields aufgeteilt wird

Email an IServ-Email-Adresse, wenn dieser Schüler einem Konto hinzugefügt wurde

register_otp: wenn eine zahl entfernt wird durch backslash (?), muss wieder in das letzte feld gesprungen werden